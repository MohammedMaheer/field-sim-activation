server {
    listen 80;
    server_name relay-client.187-127-162-233.sslip.io;
    location /.well-known/acme-challenge/ { root /opt/relay-client/acme; }
    location / { return 301 https://$host$request_uri; }
}
