# Relay Field

See the root README for setup, credentials, API URLs, offline storage and release limitations.

Android debug APK builds use a demo/emulator API URL; configure API_URL explicitly for a physical device. Do not distribute a debug-signed APK as a production release.
